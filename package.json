{
  "name": "full-page-screenshot",
  "version": "1.1.0",
  "description": "Chrome extension for capturing, annotating and exporting full-page screenshots",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "build": "./build.sh",
    "test:e2e": "./tests/e2e/run.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
